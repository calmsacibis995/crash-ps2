#include "numem.h"
#include "nuerror.h"

/*
 * @unimplemented
 *
 * Note: Instead of suppling just size of allocated memory, a filename
 * pointer and line number are also specified. Probably a debug leftover.
 */
int *
NuMemAllocFn(unsigned int size, const char *filename, const int lineno)
{
}
